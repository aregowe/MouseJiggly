<!--
    App.xaml
    
    This is the application-level XAML file that defines global resources and startup behavior.
    
    PURPOSE:
    - Defines the entry point for the WPF application
    - Specifies which window to show when the application starts (MainWindow.xaml)
    - Can contain application-wide resources like styles, brushes, and data templates
    
    STARTUP FLOW:
    1. Windows launches the application
    2. .NET runtime loads App.xaml and App.xaml.cs
    3. Application.Startup event fires (if subscribed)
    4. StartupUri="MainWindow.xaml" tells WPF to create and show MainWindow
    5. MainWindow constructor runs, UI appears
    
    RESOURCE MANAGEMENT:
    - Application.Resources section can define global styles/resources
    - Currently empty - all styling is in MainWindow.xaml
    - Could be extended for theming, custom fonts, or shared brushes
    
    NAMESPACE DECLARATIONS:
    - xmlns: Default WPF namespace (PresentationFramework)
    - xmlns:x: XAML language namespace (for x:Class, x:Name, etc.)
-->

<Application x:Class="MouseJiggly.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    
    <!-- 
        APPLICATION RESOURCES
        
        This section can contain application-wide resources such as:
        - Styles: Define consistent appearance for controls across all windows
        - Brushes: Reusable colors and gradients
        - Data Templates: Define how data objects should be displayed
        - Converters: Value converters for data binding
        
        CURRENT STATE:
        Empty - Mouse Jiggly uses default WPF styling and system colors.
        
        FUTURE EXTENSIONS:
        Could add themes (Light/Dark mode), custom fonts, or color schemes here.
        
        Example:
        <Application.Resources>
            <SolidColorBrush x:Key="AccentColor" Color="#007ACC"/>
            <Style TargetType="CheckBox">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Application.Resources>
    -->
    <Application.Resources>
         
    </Application.Resources>
</Application>
